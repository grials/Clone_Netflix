import { initialContextAuth as initialContext } from "../object/initialContextAuth";

export function authReducer(state = initialContext, action) {
  switch (action.type) {
    default:
      return state;
  }
}
